To run the applet open "TreeApplet.html". Start by creating a new tree before doing anything else.

Known issues:

- If the picture doesnt refresh or glitches out, minimize the window and restore it.
- Deletion of leaves for standard binary tree doesn't work.